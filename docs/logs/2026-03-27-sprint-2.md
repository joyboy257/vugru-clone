# Sprint Log — 2026-03-27

**Sprint:** 2
**Goal:** Complete P1 + P2 gap items from v1 roadmap
**Status:** In progress

---

## Agent Assignments

| Agent | Domain | Items |
|-------|--------|-------|
| `auth-agent` | Auth | Password reset, Google OAuth, Email+pass login, Magic link rate limiting |
| `core-agent` | Core Loop | Verify CogVideoX, Batch clip generation, Resolution selector UI |
| `billing-agent` | Billing + Auto-edit UX | Invoice history page, Credit toast, Title AI, Music AI, Auto-edit status page |
| `ops-agent` | Secondary | Duplicate project, Bulk delete, Project thumbnails, Email notification |

---

## Daily Log

### 2026-03-27 — Sprint start

Documentation framework created: `docs/spec-template.md`, `docs/logs/` directory.
Roadmap: `docs/roadmap/v1.md` — 12 items remaining across P1 and P2.

Agents dispatched at ~02:55 UTC.

### 2026-03-27 — Auth features completed

**By auth-agent:**

**Specs written (4):**
- `docs/specs/password-reset.md` — Password reset via email with 5-min token expiry
- `docs/specs/google-oauth.md` — Google OAuth with account linking logic
- `docs/specs/email-password-login.md` — Email+password alongside magic link
- `docs/specs/magic-link-rate-limiting.md` — Redis sliding window + in-memory fallback

**Schema changes (`lib/db/schema.ts`):**
- Added `googleId`, `googleEmail` columns to `users` table
- Added `passwordResetTokens` table with `lookupKey` for efficient token lookup

**New API routes:**
- `POST /api/auth/forgot-password` — rate-limited, sends reset email via Resend
- `POST /api/auth/reset-password` — verifies token, updates password, invalidates sessions
- `GET /api/auth/google` — initiates Google OAuth flow with CSRF state cookie
- `GET /api/auth/google/callback` — handles OAuth callback, creates/links user
- `PATCH /api/auth/password` — change password for logged-in users

**Updated API routes:**
- `POST /api/auth/login` — now supports password field; rate-limited (5/IP, 3/email per 15min)
- `POST /api/auth/signup` — now accepts optional `password` field (bcrypt hashed)

**New infrastructure:**
- `lib/logger.ts` — structured JSON logging with named loggers per domain
- `lib/rate-limit.ts` — sliding window rate limiter (Redis with in-memory fallback)
- `lib/email.ts` — Resend email helpers for password reset and magic link

**Env vars added:** `JWT_SECRET`, `RESEND_API_KEY`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, `REDIS_URL`

### 2026-03-27 — Core loop completed

**By core-agent:**

**Discovery:** CogVideoX via Replicate is legacy/dead code — not wired into `gpu-worker/src/clipProcessor.ts`. Active pipeline is Runway Gen-3 Alpha Turbo (`gen3a_turbo`) + Stable Video Diffusion (`stabilityai/stable-video-diffusion-img2vid`).

**Already implemented (prior agent work):**
- `POST /api/projects/[id]/clips/batch-generate` — full batch generation with credit deduction, BullMQ enqueue, 202 response
- `scripts/test-clip-generation.ts` — informational test script documenting Runway (`gen3a_turbo`) and SVD (`stabilityai/stable-video-diffusion-img2vid`) model IDs, with CogVideoX marked as legacy
- `app/(app)/project/[id]/ProjectEditorClient.tsx` — "Generate All Clips" button in Clips tab with batch modal (resolution pill toggle, motion style selector, dynamic credit cost)

**Deprecation notes added:**
- `gpu-worker/src/providers/index.ts` — added NOTE comment clarifying CogVideoX is deprecated
- `workers/video-render/src/replicate.ts` — added DEPRECATED banner comment

**Docs updated:** `docs/roadmap/v1.md` already had items marked done. Sprint log updated.

### 2026-03-27 — Billing + Auto-edit UX completed

**By billing-agent:**

**Invoice history page:**
- `app/(app)/dashboard/invoices/page.tsx` + `InvoicesClient.tsx` — lists last 20 paid Stripe invoices with date, amount, status badge, and hosted invoice link

**Credit purchase toast:**
- `app/(app)/dashboard/billing/BillingClient.tsx` — detects `?credits_added=N` URL param, shows success banner, strips param with `window.history.replaceState` (no page reload)

**AI title generation:**
- `POST /api/auto-edits/[id]/generate-title` — Cohere Vision describes first photo → Groq generates cinematic title (max 6 words, no punctuation)
- "Generate with AI" button in render modal with "AI Generated" badge

**AI music recommendation:**
- `POST /api/auto-edits/[id]/recommend-music` — Groq selects best matching preset from `lib/music.ts` based on project name
- "AI Recommended" badge on matched track, auto-selected on modal open

**Auto-edit render status page:**
- `app/(app)/project/[id]/auto-edit/[autoEditId]/page.tsx` + `AutoEditStatusClient.tsx` — 4-step progress indicator (Assembling → Rendering → Finalizing → Done), video preview + download when done, polls every 3s

**Docs updated:** `docs/plans/billing-autoedit-ux.md` created, `docs/roadmap/v1.md` all 5 items marked complete.

### 2026-03-27 — Ops features completed

**By ops-agent:**

**Specs written (4):**
- `docs/plans/ops-duplicate-project.md` — Duplicate project with deep R2 copy
- `docs/plans/ops-bulk-delete-projects.md` — Bulk delete with all-or-nothing ownership check
- `docs/plans/ops-project-thumbnails.md` — Auto-set thumbnail from first photo
- `docs/plans/ops-clip-complete-email.md` — Email notification when clip is done

**New API routes:**
- `POST /api/projects/[id]/duplicate` — Deep copy project, photos, clips (draft), auto-edits (draft), re-uploads all R2 files
- `POST /api/projects/bulk-delete` — Atomic bulk delete with DB transaction, R2 cleanup

**Thumbnail logic added to:**
- `app/api/upload/confirm/route.ts` — Sets `thumbnailUrl` when first photo is added to empty project
- `app/api/photos/[id]/route.ts` — Updates `thumbnailUrl` to next photo when thumbnail photo is deleted

**Email notification (clip done):**
- `gpu-worker/src/email.ts` — New `sendClipCompleteEmail(to, projectName, projectUrl)` function
- `gpu-worker/src/index.ts` — Calls email helper when clip status → 'done', feature-flagged via `CLIP_COMPLETE_EMAIL_ENABLED`
- `gpu-worker/package.json` — Added `resend` dependency
- `gpu-worker/src/logger.ts` — Added `clip` domain logger for `clip.complete.email_sent` / `clip.complete.email_failed`

**Logger updates:**
- `lib/logger.ts` — Added domain loggers: `project`, `projects`, `clip`, `email`
- All new routes use structured logging events: `project.duplicate.started/completed/failed`, `projects.bulk_delete.started/completed`

**Docs updated:**
- `docs/roadmap/v1.md` — All 4 secondary features marked complete
- `docs/logs/2026-03-27-sprint-2.md` — This entry
